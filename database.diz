create table dish (
	dish_id serial,
	dish_name varchar (100),
	type_id bigint unsigned not null,
	receipt text,
	primary key (dish_id)
);

create table type (
	type_id serial,
	type_name varchar (50),
	primary key (type_id)
);


insert type values ('0', 'первое блюдо'), ('1','второе блюдо');



